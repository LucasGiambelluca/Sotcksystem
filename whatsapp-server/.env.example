SUPABASE_URL=your_supabase_url
SUPABASE_KEY=your_supabase_anon_key
# Use Service Role Key only if absolutely necessary for backend admin tasks, 
# otherwise use Anon Key and rely on Row Level Security or custom middleware.
# Actually, the backend uses this key to create a client. If using 'getUser', Anon key is fine.
# But for admin tasks (like managing slots), Service Role might be needed.
# Let's verify what 'database.js' uses. It uses 'SUPABASE_KEY'.

REDIS_URL=redis://localhost:6379
GOOGLE_MAPS_API_KEY=your_google_maps_key
PORT=3001
FRONTEND_URL=http://localhost:5173
