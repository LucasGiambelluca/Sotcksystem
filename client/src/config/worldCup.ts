// Temporal World Cup 2026 theming for the public catalog.
// Flip this single flag to false (or delete the file's usages) once the
// tournament is over — it gates the loader video AND the festive background.
export const WORLD_CUP_2026 = true;

// Argentina palette — celeste y blanco, with the sun gold as an accent.
// (White pennants stay visible thanks to the celeste bunting bar + drop shadow.)
export const WC_COLORS = ['#74acdf', '#ffffff', '#74acdf', '#f6b40e'];
